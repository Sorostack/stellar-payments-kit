name: Todo to Issue

on:
  push:
    branches: [main]

jobs:
  todo-to-issue:
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - uses: actions/checkout@v4
      - uses: alstr/todo-to-issue-action@v4
        with:
          AUTO_ASSIGN: true
